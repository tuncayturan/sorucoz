"use client";

/**
 * FCM Token yönetim component'i (PASİF hale getirildi)
 * Kullanıcı artık bildirim uyarısı görmek istemediği için bu component hiçbir şey render etmez.
 */
export default function FCMTokenManager() {
  return null;
}
